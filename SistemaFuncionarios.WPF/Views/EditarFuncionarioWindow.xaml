<Window x:Class="SistemaFuncionarios.WPF.Views.EditarFuncionarioWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editar Funcionário"
        Height="250" Width="350">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nome:" Grid.Row="0"/>
        <TextBox x:Name="txtNome" Grid.Row="0" Margin="0 20 0 5"/>

        <TextBlock Text="Cargo:" Grid.Row="1"/>
        <TextBox x:Name="txtCargo" Grid.Row="1" Margin="0 20 0 5"/>

        <TextBlock Text="Salário:" Grid.Row="2"/>
        <TextBox x:Name="txtSalario" Grid.Row="2" Margin="0 20 0 5"/>

        <Button Content="Salvar"
                Grid.Row="3"
                Padding="10"
                HorizontalAlignment="Right"
                Click="Salvar_Click"/>
    </Grid>
</Window>